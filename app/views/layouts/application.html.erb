<!DOCTYPE html>
<html>
  <head>
    <title>Gotruck</title>
    <%= csrf_meta_tags %>
<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, method: :delete, data: { confirm: '로그 아웃 하시겠습니까?' }) %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>  
  </li>
<% end %>
    <%= stylesheet_link_tag    'application', media: 'all'%>
    <%= javascript_include_tag 'application'%>
  </head>

  <body>
    <div class="container">
      <%= yield %>
    </div>
</html>
