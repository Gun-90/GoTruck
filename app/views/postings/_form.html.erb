<%= form_with(model: posting, local: true) do |form| %>
  <% if posting.last.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posting.last.errors.count, "error") %> prohibited this posting from being saved:</h2>

      <ul>
      <% posting.last.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :posting_title %>
    <%= form.text_field :posting_title, id: :posting_posting_title %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
